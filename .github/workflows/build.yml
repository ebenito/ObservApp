name: Build ObservApp

on:
  push:
    branches: [ "develop", "master" ]
  pull_request:
    branches: [ "develop", "master" ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Install MAUI workload
        run: dotnet workload install maui

      - name: Install GitVersion tool
        run: dotnet tool install -g GitVersion.Tool

      - name: Calculate version with GitVersion
        id: gitversion
        run: |
          # GitVersion con /output buildserver establece env vars automáticamente
          # en GitHub Actions: GITVERSION_*
          dotnet-gitversion /output buildserver /nofetch

          # Variables ya están disponibles en env, extraer algunos valores
          Write-Host "GitVersion ejecutado correctamente" -ForegroundColor Green
          Write-Host "Variables disponibles en env:" -ForegroundColor Cyan
          Get-ChildItem env: | Where-Object { $_.Name -like "GITVERSION*" } | ForEach-Object {
            Write-Host "  $($_.Name): $($_.Value)" -ForegroundColor Gray
          }
        shell: pwsh

      - name: Crear appsettings.Local.json para MAUI
        run: |
          echo '{"SyncfusionLicenseKey": "${{ secrets.SYNCFUSION_LICENSE_KEY }}"}' > ObservApp/appsettings.Local.json

      - name: Crear appsettings.Local.json para Web Host
        run: |
          echo '{
            "SupabaseUrl": "${{ secrets.SUPABASE_URL }}",
            "SupabaseAnonKey": "${{ secrets.SUPABASE_ANON_KEY }}"
          }' > ObservApp.Web/appsettings.Local.json

      - name: Restore Android
        run: dotnet restore ObservApp/ObservApp.csproj -p:TargetFramework=net10.0-android

      - name: Restore Windows
        run: dotnet restore ObservApp/ObservApp.csproj -p:TargetFramework=net10.0-windows10.0.26100.0 -p:RuntimeIdentifier=win-x64

      - name: Build Android
        run: dotnet build ObservApp/ObservApp.csproj --framework net10.0-android --configuration Release

      - name: Publish Android
        run: dotnet publish ObservApp/ObservApp.csproj --framework net10.0-android --configuration Release --no-restore --output ObservApp/publish/android

      - name: Build Windows
        run: dotnet build ObservApp/ObservApp.csproj --framework net10.0-windows10.0.26100.0 --configuration Release --no-restore

      - name: Publish Windows
        run: dotnet publish ObservApp/ObservApp.csproj --framework net10.0-windows10.0.26100.0 --configuration Release --no-restore --output ObservApp/publish/windows

      - name: Upload Android APK
        uses: actions/upload-artifact@v5
        with:
          name: ObservApp-Android
          path: ObservApp/publish/android/*.apk
          if-no-files-found: warn

      - name: Upload Windows MSIX
        uses: actions/upload-artifact@v5
        with:
          name: ObservApp-Windows
          path: ObservApp/publish/windows/*.msix
          if-no-files-found: warn

  deploy-web:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/develop')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Restore Web Client (solo net10.0)
        run: dotnet restore ObservApp.Web.Client/ObservApp.Web.Client.csproj -p:TargetFramework=net10.0

      - name: Publish Blazor WASM standalone
        run: >
          dotnet publish ObservApp.Web.Client/ObservApp.Web.Client.csproj
          --configuration Release
          --output ./publish/web
          --no-restore
          -p:TargetFramework=net10.0
          -p:DefineConstants=STANDALONE_WASM
          -p:DebuggerSupport=false
          -p:CopyOutputSymbolsToPublishDirectory=false
          -p:Version=${{ env.GITVERSION_FULLSEMVER }}
          -p:InformationalVersion=${{ env.GITVERSION_INFORMATIONALVERSION }}

      - name: Limpiar artefactos no necesarios para Azure Static Web Apps
        run: |
          find ./publish/web/wwwroot -type f \( -name "*.pdb" -o -name "*.dbg" -o -name "*.xml" -o -name "*.map" \) -print -delete

      - name: Reducir tamaño de artefactos web para Azure SWA
        run: |
          LIMIT_BYTES=$((250*1024*1024))
          TARGET_BYTES=$((248*1024*1024))

          SIZE=$(du -sb ./publish/web/wwwroot | cut -f1)
          SIZE_MB=$(awk "BEGIN { printf \"%.1f\", $SIZE/1024/1024 }")
          echo "Tamaño inicial: ${SIZE_MB} MB"

          if [ "$SIZE" -gt "$LIMIT_BYTES" ]; then
            echo "=== Recorte 1: imágenes > 1 MB ==="
            while [ "$SIZE" -gt "$TARGET_BYTES" ]; do
              CANDIDATE=$(find ./publish/web/wwwroot -type f \( -iname "*.png" -o -iname "*.jpg" -o -iname "*.jpeg" -o -iname "*.webp" -o -iname "*.gif" -o -iname "*.svg" \) -size +1024k -printf '%s\t%p\n' | sort -nr | head -n 1)
              if [ -z "$CANDIDATE" ]; then
                echo "No quedan imágenes > 1 MB para recortar."
                break
              fi

              FILE=$(echo "$CANDIDATE" | cut -f2-)
              BYTES=$(echo "$CANDIDATE" | cut -f1)
              FILE_MB=$(awk "BEGIN { printf \"%.2f\", $BYTES/1024/1024 }")
              echo "Eliminando imagen: $FILE (${FILE_MB} MB)"
              rm -f "$FILE"
              SIZE=$(du -sb ./publish/web/wwwroot | cut -f1)
            done
          fi

          if [ "$SIZE" -gt "$LIMIT_BYTES" ]; then
            echo "=== Recorte 2: ficheros precomprimidos (*.br, *.gz) ==="
            find ./publish/web/wwwroot -type f \( -name "*.br" -o -name "*.gz" \) -print -delete || true
            SIZE=$(du -sb ./publish/web/wwwroot | cut -f1)
          fi

          SIZE_MB=$(awk "BEGIN { printf \"%.1f\", $SIZE/1024/1024 }")
          echo "Tamaño tras recortes: ${SIZE_MB} MB"

      - name: Verificar estructura y tamaño
        run: |
          LIMIT_BYTES=$((250*1024*1024))
          echo "=== wwwroot raíz ==="
          ls ./publish/web/wwwroot/
          echo "=== _framework (primeros 10) ==="
          ls ./publish/web/wwwroot/_framework/ | head -10
          echo "=== Top 20 ficheros más grandes ==="
          find ./publish/web/wwwroot -type f -printf '%s\t%p\n' | sort -nr | head -20
          SIZE=$(du -sb ./publish/web/wwwroot | cut -f1)
          SIZE_MB=$(awk "BEGIN { printf \"%.1f\", $SIZE/1024/1024 }")
          echo "Tamaño total del paquete: ${SIZE_MB} MB"

          if [ "$SIZE" -gt "$LIMIT_BYTES" ]; then
            echo "Error: El paquete supera el límite de 250 MB de Azure Static Web Apps (${SIZE_MB} MB)"
            exit 1
          fi

      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          app_location: ./publish/web/wwwroot
          skip_app_build: true
          output_location: ""